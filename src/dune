(include_subdirs no)
(executable
  (name dlgcompile)
  (modules dlgcompile)
  (libraries utils DLG typing optimiser compilation))
(executable
  (name dlgvm)
  (modules dlgvm)
  (libraries utils DLG lambda-term vm))
